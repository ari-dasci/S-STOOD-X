# Tasks: C1 — uv migration (env + dependency management)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines (handwritten) | ~65 (pyproject.toml ~25, .python-version 1, README.md ~39) |
| Estimated changed lines (with uv.lock) | ~1065 (handwritten + uv.lock ~1000) |
| Review burden lines (excl. uv.lock) | ~65 |
| 400-line budget risk | Low (against 800-line author budget) |
| Chained PRs recommended | No (for this change) |
| Suggested split | Single PR — PR #1 in the C1→C2→C3 chain |
| Delivery strategy | force-chained (author-set) |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: stacked-to-main
400-line budget risk: Low

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | Full C1: pyproject split, .python-version, uv lock/sync, README rewrite | PR #1 (chain start → main) | Self-contained config+docs change. ~65 handwritten lines + generated uv.lock. Reviewer skips uv.lock (auto-generated). |

> **uv.lock note**: Auto-generated by `uv lock`, typically 800–1200 lines. Committed for reproducibility. NOT reviewable hand-written content — exclude from cognitive review budget. Reviewer should verify it exists and `uv sync` reproduces it, not read it line-by-line.

## Phase 1: Manifest Re-expression (Steps A–B)

- [x] 1.1 Edit `pyproject.toml` `[project.dependencies]`: remove the 12 dev-only packages (pytest, sphinx, sphinx_rtd_theme, sphinxcontrib.bibtex, nbsphinx, pandoc, twine, IPython, ipywidgets, tensorboard, torchsummary, wget) and the inline OpenOOD git URL; replace with bare `"openood"`. Result: 19 runtime deps. **DoD**: `dependencies` list has exactly 19 entries ending with `"openood",` and `"libmr",`; no dev packages remain.
- [x] 1.2 Append `[dependency-groups] dev` table with the 12 removed dev packages (exact spellings from proposal Locked Decisions #3). **DoD**: Section exists with all 12 packages in alphabetical-adjacent order.
- [x] 1.3 Append `[tool.uv.sources]` table pinning `openood` to `rev = "3c35632ee91b54b09d1f085d04f94744cece7d0b"`. **DoD**: Table present with single entry; no torch entry (CPU from PyPI per D2).
- [x] 1.4 Verify `[build-system]`, `[tool.setuptools]`, `[project.urls]` are untouched. **DoD**: `git diff pyproject.toml` shows only dep split + new tables, no other changes.
- [x] 1.5 Create `.python-version` with content `3.10` (no trailing specifiers). **DoD**: File exists at repo root with single line `3.10`.

## Phase 2: Lock Generation (Step C — canary)

- [x] 2.1 Run `uv lock` from repo root. **DoD**: `uv.lock` generated; no resolver error. If OpenOOD transitive conflict (e.g. numpy version): HALT, do NOT proceed to Phase 3. Escalation: add scoped `[[tool.uv.resolution]]` override for conflicting transitive (preferred), or bump STOODX pin (author sign-off only per design Risk Handling).

## Phase 3: Sync & Verify (Steps D, F)

- [x] 3.1 Run `uv sync` from repo root. **DoD**: Exit 0, `.venv/` created, editable package + dev group installed.
- [x] 3.2 Run `uv run python -c "import STOODX"`. **DoD**: No `ImportError` / `ModuleNotFoundError` attributable to environment.
- [-] 3.3 Run `uv run pytest --collect-only`. **DEFERRED**: 5 modules fail to collect via `imgaug`→`np.sctypes` (removed in NumPy 2.0), transitive via OpenOOD `draem_preprocessor`. Accepted as a known limitation of C1 per author decision (b); documented in README "Known limitations" and annotated DEFERRED in `specs/environment/spec.md`. Resolution tracked for a follow-up change (C2/C3 scope).

## Phase 4: Documentation (Step E)

- [x] 4.1 Rewrite README.md `## Installation` section (lines 41–67): replace `pip install -e .` + 6 manual `pip install` lines with `uv sync` / `uv run pytest` workflow; add prerequisites (uv link, `build-essential` for libmr); add CUDA opt-in recipe (`UV_PYTHON_INDEX_URL`). **DoD**: No `pip install` commands remain in install section; uv workflow is primary; CUDA is documented as opt-in only.

## Phase 5: Commit

- [x] 5.1 Stage and commit: `git add pyproject.toml .python-version uv.lock README.md`. **DoD**: Single commit with exactly these 4 files; `git diff HEAD~1 --stat` shows expected files only.
